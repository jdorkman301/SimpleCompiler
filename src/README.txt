Static Semantics: LOCAL
Storage: LOCAL

'make' to make.
'make clean' to clean.

input files must end if .fs17

To run the included testfile (testfile.fs17), type
'./comp testfile'
which, if successful will create a 
'out.asm'
and you will run that through the assembler with 
'./virtMach out.asm'


